<!doctype html>
<html
  class="no-js{% if request.design_mode %} shopify-design-mode{% endif %}"
  lang="{{ request.locale.iso_code }}"
>
  <head>
    {%- if settings.favicon != blank -%}
      <link
        rel="icon"
        type="image/png"
        href="{{ settings.favicon | image_url: width: 32, height: 32 }}"
      >
    {%- endif -%}

    {% comment %} This a way to wait for main content to load when navigating to a new page so that the view transitions can work consistently {% endcomment %}
    {% # theme-check-disable RemoteAsset %}
    <link
      rel="expect"
      href="#MainContent"
      blocking="render"
      id="view-transition-render-blocker"
    >
    {% # theme-check-enable RemoteAsset %}

    {%- render 'meta-tags' -%}
    {%- render 'json-ld-organization' -%}
    {% # theme-check-disable RemoteAsset %}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Montserrat:wght@600;700;800;900&display=swap" rel="stylesheet">
    {% # theme-check-enable RemoteAsset %}
    {%- render 'stylesheets' -%}
    {%- render 'fonts' -%}
    {%- render 'scripts' -%}
    {%- render 'theme-styles-variables' -%}
    {%- render 'color-schemes' -%}
    <style>
      .pawsafe-product-conversion .pawsafe-product-conversion__header,
      .pawsafe-product-conversion .pawsafe-product-conversion__trust,
      .pawsafe-product-conversion .pawsafe-product-conversion__grid {
        box-sizing: border-box;
        width: min(100%, 1100px);
        margin-right: auto;
        margin-left: auto;
        padding-right: 28px;
        padding-left: 28px;
      }

      .pawsafe-product-conversion .pawsafe-product-conversion__header {
        max-width: none;
      }

      .pawsafe-product-conversion .pawsafe-product-conversion__header h2,
      .pawsafe-product-conversion .pawsafe-product-conversion__header p {
        max-width: 760px;
      }

      .pawsafe-product-conversion .pawsafe-product-conversion__faq,
      .pawsafe-product-conversion .pawsafe-product-conversion__reviews {
        box-sizing: border-box;
        width: min(100%, 980px);
        margin-right: auto;
        margin-left: auto;
      }

      .pawsafe-product-conversion .pawsafe-product-conversion__faq {
        padding: 18px 28px 8px;
      }

      .pawsafe-product-conversion .pawsafe-product-conversion__reviews {
        margin-top: 34px;
      }

      @media screen and (max-width: 749px) {
        .pawsafe-product-conversion .pawsafe-product-conversion__header,
        .pawsafe-product-conversion .pawsafe-product-conversion__trust,
        .pawsafe-product-conversion .pawsafe-product-conversion__grid {
          padding-right: 16px;
          padding-left: 16px;
        }

        .pawsafe-product-conversion .pawsafe-product-conversion__faq {
          padding-right: 16px;
          padding-left: 16px;
        }
      }
    </style>

    {% if request.design_mode %}
      {%- render 'theme-editor' -%}
    {% endif %}

    {{ content_for_header }}
  </head>

  <body class="page-width-{{ settings.page_width }} card-hover-effect-{{ settings.card_hover_effect }}">
    {% section 'custom-announcement-bar' %}
    {% render 'skip-to-content-link', href: '#MainContent', text: 'accessibility.skip_to_text' %}
    {% section 'pawsafe-header' %}
    {% unless template.suffix == 'faq' or template.suffix == 'contact' or template.suffix == 'about-us' or template.suffix == 'track-order' or template.name == 'product' or template.name == 'cart' %}
      {% render 'pawsafe-discount-popup' %}
    {% endunless %}
    {% render 'pawsafe-cart-polish' %}

    {% if request.page_type == 'index' %}
      <main
        id="MainContent"
        class="content-for-layout"
        role="main"
        data-page-transition-enabled="false"
        data-product-transition="false"
        data-template="index"
      >
        {{ content_for_layout }}
      </main>

      {% unless template.suffix == 'contact' %}
        {% sections 'footer-group' %}
      {% endunless %}
    {% else %}
      <script
        src="{{ 'critical.js' | asset_url }}"
        type="module"
        async
        blocking="render"
      ></script>



      <main
        id="MainContent"
        class="content-for-layout"
        role="main"
        data-page-transition-enabled="{{ settings.page_transition_enabled }}"
        data-product-transition="{{ settings.transition_to_main_product }}"
        data-template="{{ template.name }}"
      >
        {{ content_for_layout }}
      </main>

      {% unless template.suffix == 'contact' %}
        {% sections 'footer-group' %}
      {% endunless %}

      {% render 'search-modal' %}

      {% if settings.quick_add or settings.mobile_quick_add %}
        {% render 'quick-add-modal' %}
      {% endif %}
    {% endif %}
  </body>
</html>
